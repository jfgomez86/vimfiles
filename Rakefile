desc "updates with scrooloses repo (vimfiles and snippets)"
task :update do
  system("git pull http://github.com/scrooloose/vimfiles.git master")
  system("cd snippets && git pull origin master")
end
